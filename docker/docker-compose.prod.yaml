# 生产环境配置（腾讯云轻量服务器）
# 使用方式：docker compose -f docker-compose.prod.yaml up -d --build
# 与开发版差异：删除 db/redis/weaviate 的端口暴露（仅容器内网通信）、
# nginx 放开 443、GitHub 回调地址改为 .env 变量
services:
  llmops-ui:
    image: llmops-ui:0.1.0
    build:
      context: ../ui
      dockerfile: Dockerfile
    container_name: llmops-ui
    restart: always
    environment: []
    # ui不暴露宿主机端口，统一由nginx反代访问
    expose:
      - "80"
  llmops-api:
    image: llmops-api:0.1.1
    build:
      context: ../api
      dockerfile: Dockerfile
    container_name: llmops-api
    restart: always
    volumes:
      - ./volumes/app/storage:/app/api/storage
    environment:
      # 模式为API，代表启用API服务
      MODE: api
      SERVER_WORKER_AMOUNT: 1
      SERVER_THREAD_AMOUNT: 4
      # 数据库迁移配置
      MIGRATION_ENABLED: 'true'
      #jwt加密秘钥
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      # wtf配置
      WTF_CSRF_ENABLED: 'false'
      # SQLAlchemy数据库配置
      SQLALCHEMY_DATABASE_URI: ${SQLALCHEMY_DATABASE_URI}
      SQLALCHEMY_POOL_SIZE: 30
      SQLALCHEMY_POOL_RECYCLE: 3600
      SQLALCHEMY_ECHO: 'true'
      # Redis数据库配置
      REDIS_HOST: llmops-redis
      REDIS_PORT: 6379
      REDIS_USERNAME: ''
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      REDIS_DB: 0
      REDIS_USE_SSL: 'false'
      #weaviate向量数据库配置
      WEAVIATE_HTTP_HOST: llmops-weaviate
      WEAVIATE_HTTP_PORT: 8080
      WEAVIATE_GRPC_HOST: llmops-weaviate
      WEAVIATE_GRPC_PORT: 50051
      WEAVIATE_API_KEY: ${WEAVIATE_API_KEY}
      #腾讯云COS对象存储
      COS_SECRET_ID: ${COS_SECRET_ID}
      COS_SECRET_KEY: ${COS_SECRET_KEY}
      COS_REGION: ap-shanghai
      COS_SCHEME: https
      COS_BUCKET: llmops-1253288028
      COS_DOMAIN: ''
      # Celery配置
      CELERY_BROKER_DB: 1
      CELERY_RESULT_BACKEND_DB: 1
      CELERY_TASK_IGNORE_RESULT: 'true' # 或略错误，不会导致错误而中断
      CELERY_RESULT_EXPIRES: 3600
      CELERY_BROKER_CONNECTION_RETRY_ON_STARTUP: 'true'
      # Github OAuth应用秘钥（回调地址从 .env 读取，生产为域名地址）
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GITHUB_REDIRECT_URL: ${GITHUB_REDIRECT_URL}
      # langSmith配置
      LANGCHAIN_TRACING_V2: 'true'
      LANGCHAIN_ENDPOINT: https://api.smith.langchain.com
      LANGCHAIN_API_KEY: ${LANGCHAIN_API_KEY}
      LANGCHAIN_PROJECT: llmops
      # HuggingFace配置
      TRANSFORMERS_OFFLINE: 0
      # 辅助Agent智能体应用id标识
      ASSISTANT_AGENT_ID: f7effd7b-65fa-4355-a43b-7a70a331a25d
      # openai服务提供商
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_API_BASE: https://api.deepseek.com/v1
      # 智普
      GLM_API_KEY: ${GLM_API_KEY}
      GLM_API_BASE: https://open.bigmodel.cn/api/paas/v4/
      # 月之暗面
      MOONSHOT_API_KEY: ${MOONSHOT_API_KEY}
      MOONSHOT_API_BASE: https://api.moonshot.cn/v1
      # deepseek
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
      DEEPSEEK_API_BASE: https://api.deepseek.com/v1
      # 硅基流动
      SILICONFLOW_API_KEY: ${SILICONFLOW_API_KEY}
      SILICONFLOW_API_BASE: https://api.siliconflow.cn/v1
      # 中转API
      YIBU_API_URL: https://yibuapi.com/v1/
      YIBU_API_KEY: ${YIBU_API_KEY}
      #高德工具
      GAODE_API_KEY: ${GAODE_API_KEY}
      # 谷歌搜索密钥
      SERPER_API_KEY: ${SERPER_API_KEY}
    # 备案前调试用端口（防火墙不开5001即外部不可达），nginx反代走容器内网
    ports:
      - "5001:5001"
    depends_on:
      - llmops-db
      - llmops-redis
      - llmops-weaviate
  llmops-celery:
    image: llmops-api:0.1.1
    container_name: llmops-celery
    restart: always
    volumes:
      - ./volumes/app/storage:/app/api/storage
    environment:
      # 模式为celery,代表启用celery异步任务队列
      MODE: celery
      CELERY_WORKER_AMOUNT: 1
      # 数据库迁移配置
      MIGRATION_ENABLED: 'false'
      #jwt加密秘钥
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      # wtf配置
      WTF_CSRF_ENABLED: 'false'
      # SQLAlchemy数据库配置
      SQLALCHEMY_DATABASE_URI: ${SQLALCHEMY_DATABASE_URI}
      SQLALCHEMY_POOL_SIZE: 30
      SQLALCHEMY_POOL_RECYCLE: 3600
      SQLALCHEMY_ECHO: 'true'
      # Redis数据库配置
      REDIS_HOST: llmops-redis
      REDIS_PORT: 6379
      REDIS_USERNAME: ''
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      REDIS_DB: 0
      REDIS_USE_SSL: 'false'
      #weaviate向量数据库配置
      WEAVIATE_HTTP_HOST: llmops-weaviate
      WEAVIATE_HTTP_PORT: 8080
      WEAVIATE_GRPC_HOST: llmops-weaviate
      WEAVIATE_GRPC_PORT: 50051
      WEAVIATE_API_KEY: ${WEAVIATE_API_KEY}
      #腾讯云COS对象存储
      COS_SECRET_ID: ${COS_SECRET_ID}
      COS_SECRET_KEY: ${COS_SECRET_KEY}
      COS_REGION: ap-shanghai
      COS_SCHEME: https
      COS_BUCKET: llmops-1253288028
      COS_DOMAIN: ''
      # Celery配置
      CELERY_BROKER_DB: 1
      CELERY_RESULT_BACKEND_DB: 1
      CELERY_TASK_IGNORE_RESULT: 'true' # 或略错误，不会导致错误而中断
      CELERY_RESULT_EXPIRES: 3600
      CELERY_BROKER_CONNECTION_RETRY_ON_STARTUP: 'true'
      # Github OAuth应用秘钥
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GITHUB_REDIRECT_URL: ${GITHUB_REDIRECT_URL}
      # langSmith配置
      LANGCHAIN_TRACING_V2: 'true'
      LANGCHAIN_ENDPOINT: https://api.smith.langchain.com
      LANGCHAIN_API_KEY: ${LANGCHAIN_API_KEY}
      LANGCHAIN_PROJECT: llmops
      # HuggingFace配置
      TRANSFORMERS_OFFLINE: 0
      # 辅助Agent智能体应用id标识
      ASSISTANT_AGENT_ID: f7effd7b-65fa-4355-a43b-7a70a331a25d
      # openai服务提供商
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_API_BASE: https://api.deepseek.com/v1
      # 智普
      GLM_API_KEY: ${GLM_API_KEY}
      GLM_API_BASE: https://open.bigmodel.cn/api/paas/v4/
      # 月之暗面
      MOONSHOT_API_KEY: ${MOONSHOT_API_KEY}
      MOONSHOT_API_BASE: https://api.moonshot.cn/v1
      # deepseek
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
      DEEPSEEK_API_BASE: https://api.deepseek.com/v1
      # 硅基流动
      SILICONFLOW_API_KEY: ${SILICONFLOW_API_KEY}
      SILICONFLOW_API_BASE: https://api.siliconflow.cn/v1
      # 中转API
      YIBU_API_URL: https://yibuapi.com/v1/
      YIBU_API_KEY: ${YIBU_API_KEY}
      #高德工具
      GAODE_API_KEY: ${GAODE_API_KEY}
      # 谷歌搜索密钥
      SERPER_API_KEY: ${SERPER_API_KEY}
    depends_on:
      - llmops-db
      - llmops-redis
      - llmops-weaviate
  llmops-redis:
    image: redis:6-alpine
    restart: always
    container_name: llmops-redis
    volumes:
      - ./volumes/redis/data:/data
    #  启动redis服务时配置
    command: redis-server --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD","redis-cli","ping"]
    # 生产环境不暴露宿主机端口，仅容器内网访问
  llmops-db:
    image: postgres:15-alpine
    restart: always
    container_name: llmops-db
    environment:
      # 配置默认的账户名，账号密码，默认数据库，数据存储的位置
      PGUSER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: llmops
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      # 将postgres数据挂载到本地./volumes/db/data上
      - ./volumes/db/data:/var/lib/postgresql/data/pgdata
      # 初始化脚本只有在空数据挂载的情况下才触发执行(.sql/.sh等)，如果已经存在数据则不会执行
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      # 每隔1s发起一个健康测试，最多重试30次
      test: ["CMD","pg_isready"]
      interval: 1s
      timeout: 3s
      retries: 30
    # 生产环境不暴露宿主机端口，仅容器内网访问
  llmops-weaviate:
    image: semitechnologies/weaviate:1.28.4
    container_name: llmops-weaviate
    restart: always
    environment:
      QUERY_DEFAULTS_LIMIT: 25  # 查询默认返回的数据条数
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'false'  # 需要授权才能和weaviate进行交互
      PERSISTENCE_DATA_PATH: /var/lib/weaviate  # weaviate数据存储路径
      DEFAULT_VECTORIZER_MODULE: 'none'  # 向量化模块设置为none
      CLUSTER_HOSTNAME: 'node1'  # 节点的主机名字
      AUTHENTICATION_APIKEY_ENABLED: 'true'  # 启动基于API秘钥的身份校验
      AUTHENTICATION_APIKEY_ALLOWED_KEYS: ''  # 允许的API秘钥列表
      AUTHENTICATION_APIKEY_USERS: '670633108@qq.com'  # 基于秘钥的API身份列表
      AUTHORIZATION_ADMINLIST_ENABLED: 'true'  # 启动AdminList授权方案
      AUTHORIZATION_ADMINLIST_USERS: '670633108@qq.com'  # 使用AdminList方案时具有管理权限的用户
    volumes:
      - ./volumes/weaviate:/var/lib/weaviate
    # 生产环境不暴露宿主机端口，仅容器内网访问
  llmops-nginx:
    image: nginx:latest
    restart: always
    container_name: llmops-nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/proxy.conf:/etc/nginx/proxy.conf
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/ssl:/etc/ssl
    depends_on:
      - llmops-ui
      - llmops-api
    ports:
      - "80:80"
      - "443:443"
